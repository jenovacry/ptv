{
  "name": "CustomImage",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
  },
  "extensions": [ "esbenp.prettier-vscode" ],
  "postCreateCommand": "curl -fsSL https://bun.sh/install | bash && echo 'export PATH=\"/root/.bun/bin:$PATH\"' >> ~/.bashrc && echo 'alias nun=\"bun\"' >> ~/.bashrc && echo 'alias nunx=\"bunx\"' >> ~/.bashrc",
  "remoteUser": "root"
}
